sudo: required
language: python
services:
  - docker
script:
  -  docker-compose run web ./manage.py test
after_success:
  - if [ "$TRAVIS_BRANCH" == "master" ]; then
      docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD";
      docker push kalyzee/liveapi;
    fi
